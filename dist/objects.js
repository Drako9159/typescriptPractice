let carro = {
    fabricante: "Toyota",
    modelo: "Prius",
    anho: 2022,
};
let carroTypeScript;
function getCar(car) {
    console.log(`${car.fabricante} ${car.modelo} ${car.anho}`);
}
getCar(carro);

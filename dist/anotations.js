const RANDOM_TIME = Math.round(Math.random() * 500) + 500;
let startTime = new Date();
let endTime;
//Variable que puede ser de cualquier tipo
setTimeout(() => {
    endTime = 0;
    endTime = new Date();
}, RANDOM_TIME);
